version: '3.8'
services:
  nats:
    image: nats:latest
    ports:
      - "4222:4222"
  app:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - nats
gj
